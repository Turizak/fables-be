meta {
  name: Create Note
  type: http
  seq: 1
}

post {
  url: {{base_url}}/campaign/:uuid/session/:sessionUuid/note/create
  body: none
  auth: bearer
}

params:path {
  uuid: 
  sessionUuid: 
}

auth:bearer {
  token: {{token}}
}
